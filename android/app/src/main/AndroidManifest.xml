<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ==================== PERMISSIONS ==================== -->

    <!-- Internet for API calls to backend -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- SMS access for precise incoming message detection -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <!-- Notification access (granted via system settings) -->
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
        tools:ignore="ProtectedPermissions" />

    <!-- POST_NOTIFICATIONS for Android 13+ (API 33) alert notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Foreground service (if needed for long API calls) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Optional floating warning overlay. App must still be granted this in Settings. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Receive boot complete to re-enable service -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Wake lock for processing during doze (WorkManager) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- ==================== APPLICATION ==================== -->

    <application
        android:name=".ScamShieldApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="ScamShield"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ScamShield"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- ========== ACTIVITIES ========== -->

        <!-- Main Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.ScamShield">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Detail Activity (opened from notification "View Details") -->
        <activity
            android:name=".ui.DetailActivity"
            android:exported="false"
            android:parentActivityName=".ui.MainActivity" />

        <!-- ========== SERVICES ========== -->

        <!--
            NotificationListenerService — system-bound service.
            Requires user to grant notification access in Settings.
            android:permission ensures only system can bind.
        -->
        <service
            android:name=".service.ScamShieldNotificationService"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <service
            android:name=".service.ProtectionKeepAliveService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <service
            android:name=".service.ScamOverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="temporary scam warning overlay" />
        </service>



        <!-- ========== BROADCAST RECEIVERS ========== -->

        <!-- Handles notification action buttons (Block, Bait, View, Dismiss) -->
        <receiver
            android:name=".service.ScamActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.scamshield.ACTION_DISMISS" />
                <action android:name="com.scamshield.ACTION_VIEW_DETAILS" />
                <action android:name="com.scamshield.ACTION_BLOCK" />
                <action android:name="com.scamshield.ACTION_BAIT" />
            </intent-filter>
        </receiver>

        <!-- SMS Receiver for precise incoming SMS detection -->
        <receiver
            android:name=".service.SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- Boot receiver to ensure service restarts after reboot -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".ui.BaitingLogActivity"
            android:exported="false"
            android:theme="@style/Theme.ScamShield" />
        <activity
            android:name=".ui.MessageInsightsActivity"
            android:exported="false"
            android:theme="@style/Theme.ScamShield" />

        <!-- AccessibilityService for screen reading (detects messages, media, links, APKs on screen) -->
        <service
            android:name=".service.ScamShieldAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- FileProvider for sharing generated images to messaging apps -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
