fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
openai>=1.55.0
groq>=0.11.0
google-genai>=1.0.0
langdetect>=1.0.9
python-multipart>=0.0.6
httpx>=0.26.0
sqlalchemy>=2.0.30
alembic>=1.13.2
psycopg2-binary>=2.9.9
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==3.2.2
tenacity>=8.4.2
slowapi>=0.1.9
pillow>=10.3.0
numpy>=1.26.4
pytest>=8.2.0

# Inbound media understanding (VisionAnalyzer).
# OPTIONAL & HEAVY: app/agents/detection/vision_analyzer.py imports these lazily
# and degrades to a metadata-only summary if absent, so the base deploy stays
# light. Install these (plus the Tesseract system binary) only on hosts that can
# afford it, to enable real image captioning + OCR of scammer-sent images.
#   transformers>=4.40.0
#   torch>=2.2.0
#   pytesseract>=0.3.10

# Machine Learning & Dataset Generation
pandas>=2.0.0
faker>=24.0.0
scikit-learn>=1.3.0
seaborn>=0.13.0
matplotlib>=3.8.0
tensorflow>=2.15.0
mediapipe-model-maker>=0.2.1.3
